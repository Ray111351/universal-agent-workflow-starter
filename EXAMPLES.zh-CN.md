# Universal Agent Workflow v1.1：中文示例

[返回首页](./README.md) | [LITE](./PROMPT.lite.zh-CN.md) | [GOVERNED](./PROMPT.governed.zh-CN.md) | [English examples](./EXAMPLES.en.md)

以下消息可以直接发送给已经加载相应工作流的 Agent。角色和风险通常由 Agent 根据任务自动判断，无需用户背诵术语。

## 示例一：LITE 修复小问题

```text
修复用户登录后偶发返回 500 的问题。

输入：当前仓库、错误日志和已有测试。
限制：
- 不改变登录接口；
- 不新增外部依赖；
- 不触碰生产环境；
- 保留我的其他未提交改动。

定位根因后直接实施最小修复并验证。只有出现会改变范围或风险的
真实决策时再问我。
```

预期：Agent 简短说明目标、边界和验证，然后直接推进；不会重复索要批准。

## 示例二：只读公开资料研究

```text
比较三个公开可用的视频人物跟踪方案。优先读取当前官方文档和原始
论文，说明更新时间、适用限制和你的推断。本轮只研究，不安装、购买
或修改任何东西。最后给出一个推荐。
```

预期：Agent 直接研究并引用来源，不生成工作单或要求批准。

## 示例三：表格数据清洗

```text
清理 customer_feedback.xlsx：统一日期和分类格式，删除完全重复行，
保留原始工作表，并新增 cleaned 和 summary 两个工作表。

不得上传数据到外部服务，不得删除原文件。完成后报告行数变化、规则
和无法确定的记录。
```

预期：复杂度可能为 M，但动作仍可逆；Agent 先做简短计划，不进入高风险流程。

## 示例四：GOVERNED 公开发布

```text
准备发布 v1.2.0：检查变更、更新版本说明并创建候选发布内容。

可以完成所有只读检查和可逆准备，但在真正创建 tag、Release、推送、
合并或发送公开消息之前，必须向我展示精确目标、基线、内容和回滚性，
等待即时确认。
```

预期：Agent 区分“准备”和“真正发布”；一次小改动也不会绕过 EXTERNAL 风险确认。

## 示例五：顺序多 Agent

规划会话：

```text
以 PLAN 职责检查现状并生成 PROPOSED WORK ORDER。本轮不修改实现。
只有存在真实取舍时才比较多个方案，并把需要我决定的问题集中提出。
```

执行会话：

```text
以 EXECUTE 职责读取获批工作单和绑定基线。只实施可映射到工作单的
内容，验证并保存证据。若基线实质变化，把授权标为 STALE_APPROVAL。
```

独立审查会话：

```text
以 REVIEW 职责只读审查指定 diff、工作单和证据。按 P0–P3 报告问题。
无发现时写 No findings within reviewed scope，并说明未检查内容。
```

预期：这是三个顺序职责，不代表所有任务都必须调用三个 Agent。最终 `USER_ACCEPTED` 仍由用户决定。

## 示例六：明确授权高风险动作

推荐写法：

```text
批准在仓库 Ray/example 的 main@abc123 基线上创建公开 v1.2.0 Release。
目标受众为所有 GitHub 访客，不包含生产部署或对外群发消息。
```

不要只写：

```text
可以
```
